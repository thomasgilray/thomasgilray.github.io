\documentclass[11pt]{article}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[margin=1in,top=0.85in]{geometry}
\usepackage{mathpazo}
\usepackage[scaled=0.92]{inconsolata}
\usepackage{enumitem}
\usepackage{fancyhdr}
\usepackage{xcolor}
\usepackage{listings}
\usepackage{parskip}

\definecolor{codebg}{gray}{0.96}
\definecolor{note}{gray}{0.45}

\lstset{
  basicstyle=\ttfamily\small,
  backgroundcolor=\color{codebg},
  frame=none,
  xleftmargin=1.5em,
  framexleftmargin=0.7em,
  framexrightmargin=0.7em,
  framextopmargin=0.4em,
  framexbottommargin=0.4em,
  aboveskip=0.8em,
  belowskip=0.6em,
  showstringspaces=false,
  columns=fullflexible,
  keepspaces=true,
  language=Lisp,
  keywordstyle=\color{black},
  commentstyle=\color{note}\itshape,
}

\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\rhead{\small\color{note}(Example CptS 355 Study Group Report)}

\setlist[itemize]{leftmargin=1.4em,itemsep=0.3em,topsep=0.5em}

\begin{document}

{\large\bfseries CptS 355 Study Group --- Week 8}

\smallskip
\textbf{Scribe:} Claire \qquad \textbf{Attended:} Jason, Sarah, Arvind, Claire (all four)

\smallskip
Met Wednesday at 7:00pm on the ground floor of SEH, for about 60 minutes.

\begin{itemize}

\item Went back over the lambda calculus part of Tuesday's lecture.

\item Rule for free variables: free in a body if no enclosing lambda binds it.

\item Marked every variable free or bound in four small expressions. Sarah caught one.

\item A name can be free in the inner lambda and bound in the outer one.

\item A closure is the lambda plus the bindings for its free variables.

\item Returned a lambda out of a Scheme function and called it after the outer call. Tracked what happened to the free variable and checked it against beta reduction by hand.

\item Wrote down Church 0, 1, and 2, and what the two arguments are for.

\item Could not get Church addition right. Ours gave the wrong numeral.

\item Arvind is redoing addition. Jason will bring the substitution rules.

\end{itemize}

The example we spent the most time on:

\begin{lstlisting}
(define (make-adder n) (lambda (x) (+ x n)))
(define add5 (make-adder 5))   ; n is free in the inner lambda
\end{lstlisting}

By the time \texttt{add5} runs, \texttt{n} is out of scope, so the closure is carrying it.

\end{document}
